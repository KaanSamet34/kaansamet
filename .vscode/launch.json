{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open React in Integrated Browser",
      "type": "editor-browser",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "next-dev"
    }
  ]
}